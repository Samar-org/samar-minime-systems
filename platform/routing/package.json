{
  "name": "@samar/routing",
  "version": "1.0.0",
  "description": "Multi-provider model routing and selection engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@samar/config": "workspace:*",
    "@samar/observability": "workspace:*",
    "@samar/schemas": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.2.0",
    "vitest": "^0.34.0"
  }
}
